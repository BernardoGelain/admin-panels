import { Metadata } from "next";
import { HomePage } from "~/page-components/home-page/home-page";

export const metadata: Metadata = {
  title: "Zukese Admin Web - Home",
};

export default HomePage;
