import { Metadata } from "next";
import { HomePage } from "~/page-components/home-page/home-page";

export const metadata: Metadata = {
  title: "Marcante Admin Web - Home",
};

export default HomePage;
