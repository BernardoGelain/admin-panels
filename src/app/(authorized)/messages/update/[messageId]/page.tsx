import { Metadata } from "next";
import { UpdateMessagePage } from "~/page-components/crud/messages-page/components/update-message/update-message-page";

export const metadata: Metadata = {
  title: "Editar Mensagem",
};

export default UpdateMessagePage;
